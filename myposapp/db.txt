Project "None" {
  database_type: 'SQLite'
  Note: '''None
  Last Updated At 02-03-2025 08:32AM UTC'''
}

enum admin.positive_small_integer_logentry_action_flag {
  "1" [note: '''เพิ่ม''']
  "2" [note: '''เปลี่ยนแปลง''']
  "3" [note: '''ลบ''']
}

enum myapp.char_member_role {
  "owner" [note: '''เจ้าของร้าน''']
  "employee" [note: '''พนักงาน''']
}

Table admin.LogEntry {
  Note: '''
LogEntry(id, action_time, user, content_type, object_id, object_repr, action_flag, change_message)

*DB table: django_admin_log*'''

  id auto [pk, unique, not null]
  action_time date_time [default:`django.utils.timezone.now()`, not null]
  user_id foreign_key [not null]
  content_type_id foreign_key [null]
  object_id text [null]
  object_repr char [not null]
  action_flag admin.positive_small_integer_logentry_action_flag [not null]
  change_message text [not null]

  indexes {
    (content_type_id) [name: 'django_admin_log_content_type_id_c4bce8eb', type: btree]
    (id) [pk, unique, name: 'django_admin_log_pkey', type: btree]
    (user_id) [name: 'django_admin_log_user_id_c564eba6', type: btree]
  }
}
ref: admin.LogEntry.user_id > auth.User.id
ref: admin.LogEntry.content_type_id > contenttypes.ContentType.id


Table auth.Group {
  Note: '''
Groups are a generic way of categorizing users to apply permissions, or
some other label, to those users. A user can belong to any number of
groups.

A user in a group automatically has all the permissions granted to that
group. For example, if the group 'Site editors' has the permission
can_edit_home_page, any user in that group will have that permission.

Beyond permissions, groups are a convenient way to categorize users to
apply some label, or extended functionality, to them. For example, you
could create a group 'Special users', and you could write code that would
do special things to those users -- such as giving them access to a
members-only portion of your site, or sending them members-only email
messages.


*DB table: auth_group*'''

  id auto [pk, unique, not null]
  name char [unique, not null]

  indexes {
    (name) [unique, name: 'auth_group_name_key', type: btree]
    (id) [pk, unique, name: 'auth_group_pkey', type: btree]
  }
}


Table auth.Permission {
  Note: '''
The permissions system provides a way to assign permissions to specific
users and groups of users.

The permission system is used by the Django admin site, but may also be
useful in your own code. The Django admin site uses permissions as follows:

- The "add" permission limits the user's ability to view the "add" form
and add an object.
- The "change" permission limits a user's ability to view the change
list, view the "change" form and change an object.
- The "delete" permission limits the ability to delete an object.
- The "view" permission limits the ability to view an object.

Permissions are set globally per type of object, not per specific object
instance. It is possible to say "Mary may change news stories," but it's
not currently possible to say "Mary may change news stories, but only the
ones she created herself" or "Mary may only change news stories that have a
certain status or publication date."

The permissions listed above are automatically created for each model.


*DB table: auth_permission*'''

  id auto [pk, unique, not null]
  name char [not null]
  content_type_id foreign_key [not null]
  codename char [not null]

  indexes {
    (content_type_id) [name: 'auth_permission_content_type_id_2f476e4b', type: btree]
    (content_type_id,codename) [unique, name: 'auth_permission_content_type_id_codename_01ab375a_uniq', type: btree]
    (id) [pk, unique, name: 'auth_permission_pkey', type: btree]
  }
}
ref: auth.Permission.content_type_id > contenttypes.ContentType.id


Table auth.User {
  Note: '''
Users within the Django authentication system are represented by this
model.

Username and password are required. Other fields are optional.


*DB table: auth_user*'''

  id auto [pk, unique, not null]
  password char [not null]
  last_login date_time [null]
  is_superuser boolean [note: '''กำหนดสิทธิ์ทุกอย่างให้ผู้ใช้ โดยไม่ต้องการมอบหมายอย่างเป็นทางการ''', default:`False`, not null]
  username char [note: '''Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.''', unique, not null]
  first_name char [not null]
  last_name char [not null]
  email email [not null]
  is_staff boolean [note: '''กำหนดผู้ที่จะเข้ามาใช้งานในส่วนของผู้ดูแลระบบ''', default:`False`, not null]
  is_active boolean [note: '''กำหนดให้ผู้ใช้มีสถานะที่สามารถเข้ามาใช้งานได้ เลือกออกแทนที่จะลบชื่อผู้ใช้ทิ้งไป ''', default:`True`, not null]
  date_joined date_time [default:`django.utils.timezone.now()`, not null]

  indexes {
    (id) [pk, unique, name: 'auth_user_pkey', type: btree]
    (username) [unique, name: 'auth_user_username_key', type: btree]
  }
}


Table auth.group_permissions {
  Note: '''
This is a Many-To-Many linking table autogenerated by Django.

*DB table: auth_group_permissions*'''

  id auto [pk, not null]
  permission_id auto
  group_id auto

  indexes {
    (group_id) [name: 'auth_group_permissions_group_id_b120cbf9', type: btree]
    (group_id,permission_id) [unique, name: 'auth_group_permissions_group_id_permission_id_0cd325b0_uniq', type: btree]
    (permission_id) [name: 'auth_group_permissions_permission_id_84c5c92e', type: btree]
    (id) [pk, unique, name: 'auth_group_permissions_pkey', type: btree]
  }
}
ref: auth.Group.id > auth.group_permissions.group_id
ref: auth.Permission.id > auth.group_permissions.permission_id


Table auth.user_groups {
  Note: '''
This is a Many-To-Many linking table autogenerated by Django.

*DB table: auth_user_groups*'''

  id auto [pk, not null]
  group_id auto
  user_id auto

  indexes {
    (group_id) [name: 'auth_user_groups_group_id_97559544', type: btree]
    (id) [pk, unique, name: 'auth_user_groups_pkey', type: btree]
    (user_id) [name: 'auth_user_groups_user_id_6a12ed8b', type: btree]
    (user_id,group_id) [unique, name: 'auth_user_groups_user_id_group_id_94350c0c_uniq', type: btree]
  }
}
ref: auth.User.id > auth.user_groups.user_id
ref: auth.Group.id > auth.user_groups.group_id


Table auth.user_user_permissions {
  Note: '''
This is a Many-To-Many linking table autogenerated by Django.

*DB table: auth_user_user_permissions*'''

  id auto [pk, not null]
  permission_id auto
  user_id auto

  indexes {
    (permission_id) [name: 'auth_user_user_permissions_permission_id_1fbb5f2c', type: btree]
    (id) [pk, unique, name: 'auth_user_user_permissions_pkey', type: btree]
    (user_id) [name: 'auth_user_user_permissions_user_id_a95ead1b', type: btree]
    (user_id,permission_id) [unique, name: 'auth_user_user_permissions_user_id_permission_id_14a6b632_uniq', type: btree]
  }
}
ref: auth.User.id > auth.user_user_permissions.user_id
ref: auth.Permission.id > auth.user_user_permissions.permission_id


Table contenttypes.ContentType {
  Note: '''
ContentType(id, app_label, model)

*DB table: django_content_type*'''

  id auto [pk, unique, not null]
  app_label char [not null]
  model char [not null]

  indexes {
    (app_label,model) [unique, name: 'django_content_type_app_label_model_76bd3d3b_uniq', type: btree]
    (id) [pk, unique, name: 'django_content_type_pkey', type: btree]
  }
}


Table myapp.Category {
  Note: '''
Category(id, name, store)

*DB table: myapp_category*'''

  id big_auto [pk, unique, not null]
  name char [not null]
  store_id foreign_key [null]

  indexes {
    (id) [pk, unique, name: 'myapp_category_pkey', type: btree]
    (store_id) [name: 'myapp_category_store_id_5e63d12b', type: btree]
  }
}
ref: myapp.Category.store_id > myapp.Store.id


Table myapp.Ingredient {
  Note: '''
Ingredient(id, name, unit, stock, reorder_level, store)

*DB table: myapp_ingredient*'''

  id big_auto [pk, unique, not null]
  name char [not null]
  unit char [not null]
  stock decimal [default:`0`, not null]
  reorder_level decimal [default:`10`, not null]
  store_id foreign_key [null]

  indexes {
    (id) [pk, unique, name: 'myapp_ingredient_pkey', type: btree]
    (store_id) [name: 'myapp_ingredient_store_id_67112d2c', type: btree]
  }
}
ref: myapp.Ingredient.store_id > myapp.Store.id


Table myapp.Member {
  Note: '''
Member(id, user, role, store)

*DB table: myapp_member*'''

  id big_auto [pk, unique, not null]
  user_id one_to_one [unique, not null]
  role myapp.char_member_role [default:`"owner"`, not null]
  store_id foreign_key [null]

  indexes {
    (id) [pk, unique, name: 'myapp_member_pkey', type: btree]
    (store_id) [name: 'myapp_member_store_id_064a235a', type: btree]
    (user_id) [unique, name: 'myapp_member_user_id_key', type: btree]
  }
}
ref: myapp.Member.user_id - auth.User.id
ref: myapp.Member.store_id > myapp.Store.id


Table myapp.Option {
  Note: '''
Option(id, product, name, price)

*DB table: myapp_option*'''

  id big_auto [pk, unique, not null]
  product_id foreign_key [not null]
  name char [not null]
  price decimal [default:`0.0`, not null]

  indexes {
    (id) [pk, unique, name: 'myapp_option_pkey', type: btree]
    (product_id) [name: 'myapp_option_product_id_97376af1', type: btree]
  }
}
ref: myapp.Option.product_id > myapp.Product.id


Table myapp.Order {
  Note: '''
Order(id, customer_name, customer_phone, total_price, created_at, status, store, employee)

*DB table: myapp_order*'''

  id big_auto [pk, unique, not null]
  customer_name char [null]
  customer_phone char [null]
  total_price decimal [default:`0`, not null]
  created_at date_time [not null]
  status char [default:`"Pending"`, not null]
  store_id foreign_key [null]
  employee_id foreign_key [null]

  indexes {
    (employee_id) [name: 'myapp_order_employee_id_e4d49947', type: btree]
    (id) [pk, unique, name: 'myapp_order_pkey', type: btree]
    (store_id) [name: 'myapp_order_store_id_25f691db', type: btree]
  }
}
ref: myapp.Order.store_id > myapp.Store.id
ref: myapp.Order.employee_id > auth.User.id


Table myapp.OrderDetail {
  Note: '''
OrderDetail(id, order, product, quantity, price)

*DB table: myapp_orderdetail*'''

  id big_auto [pk, unique, not null]
  order_id foreign_key [not null]
  product_id foreign_key [not null]
  quantity integer [default:`1`, not null]
  price decimal [not null]

  indexes {
    (order_id) [name: 'myapp_orderdetail_order_id_41a8de1b', type: btree]
    (id) [pk, unique, name: 'myapp_orderdetail_pkey', type: btree]
    (product_id) [name: 'myapp_orderdetail_product_id_7678c35b', type: btree]
  }
}
ref: myapp.OrderDetail.order_id > myapp.Order.id
ref: myapp.OrderDetail.product_id > myapp.Product.id


Table myapp.Payment {
  Note: '''
Payment(id, order, amount, payment_method, payment_status, created_at, store)

*DB table: myapp_payment*'''

  id big_auto [pk, unique, not null]
  order_id one_to_one [unique, not null]
  amount decimal [not null]
  payment_method char [not null]
  payment_status char [default:`"Pending"`, not null]
  created_at date_time [not null]
  store_id foreign_key [null]

  indexes {
    (order_id) [unique, name: 'myapp_payment_order_id_key', type: btree]
    (id) [pk, unique, name: 'myapp_payment_pkey', type: btree]
    (store_id) [name: 'myapp_payment_store_id_8597ca94', type: btree]
  }
}
ref: myapp.Payment.order_id - myapp.Order.id
ref: myapp.Payment.store_id > myapp.Store.id


Table myapp.PointsConfig {
  Note: '''
PointsConfig(id, points_per_baht, store)

*DB table: myapp_pointsconfig*'''

  id big_auto [pk, unique, not null]
  points_per_baht decimal [default:`1.0`, not null]
  store_id foreign_key [null]

  indexes {
    (id) [pk, unique, name: 'myapp_pointsconfig_pkey', type: btree]
    (store_id) [name: 'myapp_pointsconfig_store_id_da42be80', type: btree]
  }
}
ref: myapp.PointsConfig.store_id > myapp.Store.id


Table myapp.Product {
  Note: '''
Product(id, product_name, img_product, description, price, category, store, promotion)

*DB table: myapp_product*'''

  id big_auto [pk, unique, not null]
  product_name char [not null]
  img_product image [null]
  description text [null]
  price decimal [null]
  category_id foreign_key [not null]
  store_id foreign_key [null]
  promotion_id foreign_key [null]

  indexes {
    (category_id) [name: 'myapp_product_category_id_f672ddc0', type: btree]
    (id) [pk, unique, name: 'myapp_product_pkey', type: btree]
    (promotion_id) [name: 'myapp_product_promotion_id_95863aab', type: btree]
    (store_id) [name: 'myapp_product_store_id_fa9d7e12', type: btree]
  }
}
ref: myapp.Product.category_id > myapp.Category.id
ref: myapp.Product.store_id > myapp.Store.id
ref: myapp.Product.promotion_id > myapp.Promotion.id


Table myapp.ProductIngredient {
  Note: '''
ProductIngredient(id, product, ingredient, quantity, store)

*DB table: myapp_productingredient*'''

  id big_auto [pk, unique, not null]
  product_id foreign_key [not null]
  ingredient_id foreign_key [not null]
  quantity decimal [not null]
  store_id foreign_key [null]

  indexes {
    (ingredient_id) [name: 'myapp_productingredient_ingredient_id_fa0ffdb7', type: btree]
    (id) [pk, unique, name: 'myapp_productingredient_pkey', type: btree]
    (product_id) [name: 'myapp_productingredient_product_id_d536f58c', type: btree]
    (store_id) [name: 'myapp_productingredient_store_id_9cd74535', type: btree]
  }
}
ref: myapp.ProductIngredient.product_id > myapp.Product.id
ref: myapp.ProductIngredient.ingredient_id > myapp.Ingredient.id
ref: myapp.ProductIngredient.store_id > myapp.Store.id


Table myapp.Promotion {
  Note: '''
Promotion(id, name, discount_percentage, start_date, end_date, store)

*DB table: myapp_promotion*'''

  id big_auto [pk, unique, not null]
  name char [not null]
  discount_percentage decimal [not null]
  start_date date_time [not null]
  end_date date_time [not null]
  store_id foreign_key [null]

  indexes {
    (id) [pk, unique, name: 'myapp_promotion_pkey', type: btree]
    (store_id) [name: 'myapp_promotion_store_id_35aa5456', type: btree]
  }
}
ref: myapp.Promotion.store_id > myapp.Store.id


Table myapp.Store {
  Note: '''
Store(id, name, owner)

*DB table: myapp_store*'''

  id big_auto [pk, unique, not null]
  name char [unique, not null]
  owner_id one_to_one [unique, not null]

  indexes {
    (name) [unique, name: 'myapp_store_name_key', type: btree]
    (owner_id) [unique, name: 'myapp_store_owner_id_key', type: btree]
    (id) [pk, unique, name: 'myapp_store_pkey', type: btree]
  }
}
ref: myapp.Store.owner_id - auth.User.id


Table myapp.customerMember {
  Note: '''
customerMember(id, name, email, phone, points, created_at, store)

*DB table: myapp_customermember*'''

  id big_auto [pk, unique, not null]
  name char [not null]
  email email [unique, not null]
  phone char [unique, not null]
  points positive_integer [default:`0`, not null]
  created_at date_time [not null]
  store_id foreign_key [null]

  indexes {
    (email) [unique, name: 'myapp_customermember_email_key', type: btree]
    (phone) [unique, name: 'myapp_customermember_phone_key', type: btree]
    (id) [pk, unique, name: 'myapp_customermember_pkey', type: btree]
    (store_id) [name: 'myapp_customermember_store_id_bd402c7c', type: btree]
  }
}
ref: myapp.customerMember.store_id > myapp.Store.id


Table myapp.order_products {
  Note: '''
This is a Many-To-Many linking table autogenerated by Django.

*DB table: myapp_order_products*'''

  id auto [pk, not null]
  product_id auto
  order_id auto

  indexes {
    (order_id) [name: 'myapp_order_products_order_id_04af4861', type: btree]
    (order_id,product_id) [unique, name: 'myapp_order_products_order_id_product_id_cf167c3d_uniq', type: btree]
    (id) [pk, unique, name: 'myapp_order_products_pkey', type: btree]
    (product_id) [name: 'myapp_order_products_product_id_b4d6a357', type: btree]
  }
}
ref: myapp.Order.id > myapp.order_products.order_id
ref: myapp.Product.id > myapp.order_products.product_id


Table myapp.orderdetail_options {
  Note: '''
This is a Many-To-Many linking table autogenerated by Django.

*DB table: myapp_orderdetail_options*'''

  id auto [pk, not null]
  option_id auto
  orderdetail_id auto

  indexes {
    (option_id) [name: 'myapp_orderdetail_options_option_id_1d34948d', type: btree]
    (orderdetail_id) [name: 'myapp_orderdetail_options_orderdetail_id_ff7bfa5d', type: btree]
    (orderdetail_id,option_id) [unique, name: 'myapp_orderdetail_options_orderdetail_id_option_id_a85e7d75_uniq', type: btree]
    (id) [pk, unique, name: 'myapp_orderdetail_options_pkey', type: btree]
  }
}
ref: myapp.OrderDetail.id > myapp.orderdetail_options.orderdetail_id
ref: myapp.Option.id > myapp.orderdetail_options.option_id


Table sessions.Session {
  Note: '''
Django provides full support for anonymous sessions. The session
framework lets you store and retrieve arbitrary data on a
per-site-visitor basis. It stores data on the server side and
abstracts the sending and receiving of cookies. Cookies contain a
session ID -- not the data itself.

The Django sessions framework is entirely cookie-based. It does
not fall back to putting session IDs in URLs. This is an intentional
design decision. Not only does that behavior make URLs ugly, it makes
your site vulnerable to session-ID theft via the "Referer" header.

For complete documentation on using Sessions in your code, consult
the sessions documentation that is shipped with Django (also available
on the Django web site).


*DB table: django_session*'''

  session_key char [pk, unique, not null]
  session_data text [not null]
  expire_date date_time [not null]

  indexes {
    (expire_date) [name: 'django_session_expire_date_a5c62663', type: btree]
    (session_key) [pk, unique, name: 'django_session_pkey', type: btree]
  }
}
